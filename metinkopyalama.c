#include <stdio.h>
#include <string.h>
int main()
{
	char mustafa[100];
	char halil[100];
	printf("gir lan gavat:",mustafa);
	gets(mustafa);
	str(halil,mustafa);
	printf("%s",halil);
	return 0;
		}
